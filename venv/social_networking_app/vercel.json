{
    "builds": [{
        "src": "venv/social_networking_app/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python 3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "venv/social_networking_app/wsgi.py"
        }
    ]
}
